# certificateGenerate

```bash
# Clone this repository
$ git clone https://github.com/gopherss/certificateGenerate.git

# Go into the repository
$ cd certificateGenerate

# Install dependencies
$ npm install

# Run the app
$ node ./src/cerfificate.js
```